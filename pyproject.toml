[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.ruff]
extend-exclude = [
    "contrib/ZUGFeRD/2.3.3/generated",
    "splendid_invoice/zugferd/generated",
]
